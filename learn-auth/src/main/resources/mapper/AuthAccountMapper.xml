<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.learn.mall.auth.mapper.AuthAccountMapper">

    <select id="getAuthAccountInVerifyByInputUserName" resultType="com.learn.mall.common.security.bo.AuthAccountInVerifyBO">
        select user_id, sys_type, uid, password, is_admin, tenant_id, status, username
        from auth_account
        where sys_type = #{sysType}
          and status = 1
          and username = #{inputUserName}
    </select>

</mapper>
