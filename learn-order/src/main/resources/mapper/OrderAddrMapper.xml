<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.learn.mall.order.mapper.OrderAddrMapper">

    <select id="getById" resultType="com.learn.mall.order.model.OrderAddr">
        select order_addr_id, user_id, consignee, province_id, province, city_id, city,
               area_id, area, addr, post_code, mobile
        from order_addr where order_addr_id = #{orderAddrId}
    </select>

    <insert id="save" useGeneratedKeys="true" keyProperty="orderAddrId">
        insert into order_addr (user_id, consignee, province_id, province, city_id, city,
                                area_id, area, addr, post_code, mobile)
        values (#{userId}, #{consignee}, #{provinceId}, #{province}, #{cityId}, #{city},
                #{areaId}, #{area}, #{addr}, #{postCode}, #{mobile})
    </insert>
</mapper>
