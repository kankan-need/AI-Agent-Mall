<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.learn.mall.order.mapper.OrderItemMapper">

    <select id="listByOrderId" resultType="com.learn.mall.order.model.OrderItem">
        select order_item_id, shop_id, order_id, category_id, spu_id, sku_id, user_id,
               count, spu_name, pic, price, spu_total_amount
        from order_item where order_id = #{orderId}
    </select>

    <insert id="save" useGeneratedKeys="true" keyProperty="orderItemId">
        insert into order_item (shop_id, order_id, category_id, spu_id, sku_id, user_id,
                                count, spu_name, pic, price, spu_total_amount)
        values (#{shopId}, #{orderId}, #{categoryId}, #{spuId}, #{skuId}, #{userId},
                #{count}, #{spuName}, #{pic}, #{price}, #{spuTotalAmount})
    </insert>
</mapper>
